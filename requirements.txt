# OpenTinker-Miles Training API Dependencies
#
# NOTE: This is for layering on top of Miles base image which already has:
# - torch, transformers, numpy, httpx (DO NOT override these)
# - tinker_gmi, tinker-cookbook (at /tmp/)
#
# Only add packages NOT in the base image:

# Ray - pinned version for compatibility
ray==2.52.0

# FastAPI server
fastapi[standard]
uvicorn[standard]
python-multipart

# API utilities
openai
tiktoken
pydantic>=2.0.0

# Tinker proto wire format (SDK >= 0.25): protobuf for the schema, zstandard
# for compressed forward_backward bodies (optional: without it the server
# tells clients not to compress)
protobuf>=4.21
zstandard

# Observability (optional, for production)
prometheus-client
opentelemetry-distro
opentelemetry-exporter-otlp
